
--- Recorsione ---

- Una funzione è detta ricorsiva se chiama se stessa.

 Esempio di ricorsione per calcolare il fattoriale.
 
 Formula per il fattoriale: n!=n*(n-1);
 
if(n!<=1){
  return 1; 
  }else{
return n*(n-1);
}
 
